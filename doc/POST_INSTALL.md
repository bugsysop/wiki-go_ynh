You can now log in to Wiki-Go admin page, using the **user name** you choosed with the install form. The **default password** is `admin` (generated by the installer), we strongly suggest you to _change immediately_ for obvious security reasons.
